[project]
name = "muntello-support-bot"
version = "1.0.0"
description = "Telegram support bot with ticket management"
requires-python = ">=3.11"

[project.optional-dependencies]
dev = [
    "pytest==7.4.4",
    "pytest-asyncio==0.23.3",
    "pytest-cov==4.1.0",
    "ruff==0.1.15",
    "httpx==0.26.0",
]
